71. Simplify Path

class Solution {
public:
    string simplifyPath(string path) {
        // 📦 【狀態容器：模擬路徑堆疊】
        // 記錄目前絕對路徑中的每一個「有效資料夾名稱」
        vector<string> stk;
        
        // ✂️ 【字串切割器準備】
        string token;
        stringstream ss(path); // 將字串包裝成資料流，方便後續用分隔符號讀取

        // 🔄 【第一階段：解析與過濾】
        // 使用 getline，以 '/' 為分隔符號，一段一段把字串切出來放到 token 中
        // 例如 "/a//b/c/" 會被切成 "", "a", "", "b", "c"
        while (getline(ss, token, '/')) {
            
            // 🛡️ 狀況 1：無效指令 (原地踏步)
            // "" 是因為連續的斜線 (如 //) 切割出來的空字串
            // "." 代表當前目錄，這兩者都不會改變路徑深度，直接忽略
            if (token == "" || token == ".") {
                continue;
            } 
            // 🔙 狀況 2：返回上一層 (退後一步)
            // ".." 代表回到上一層目錄
            else if (token == "..") {
                // 防禦機制：必須確保目前堆疊裡有東西才能退。
                // 如果已經在根目錄 (stk 為空)，再遇到 ".." 還是只能留在根目錄，所以忽略。
                if (!stk.empty()) {
                    stk.pop_back(); // 拔除最後進入的資料夾
                }
            } 
            // 📥 狀況 3：進入新資料夾 (往前一步)
            // 排除以上狀況後，剩下的就是正常的資料夾名稱
            else {
                stk.push_back(token);
            }
        }

        // 🏁 【第二階段：路徑重組】
        
        // 極端邊界防衛：如果一頓操作之後，堆疊被清空了 (例如 "/../" 或 "//")
        // 代表我們回到了最頂層的根目錄，直接回傳 "/"
        if (stk.empty()) return "/";

        string result;
        // 由於我們是用 vector 模擬堆疊，這裡可以直接「由底層到頂層 (由左至右)」順向遍歷
        for (string& dir : stk) {
            result += "/" + dir; // 每遇到一個資料夾，就在前面補上 '/'
        }
        
        return result;
    }
};