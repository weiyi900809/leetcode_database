155. Min Stack

class MinStack {
public:
    // 📦 【資料結構定義】
    // 嚴格遵守「單一真實來源 (Single Source of Truth)」原則，不使用額外變數。
    stack<int> main_stk; // 主堆疊：負責記錄所有真正被推進來的原始數據。
    stack<int> min_stk;  // 影子堆疊：負責記錄「當主堆疊擁有 N 個元素時，對應的歷史最小值」。

    MinStack() {
        // 因為我們完全依賴 stack 本身的狀態，不需要額外初始化常數，保持乾淨。
    }
    
    void push(int value) {
        // 1. 真實數據無條件進入主堆疊
        main_stk.push(value);
        
        // 2. 更新影子堆疊 (維護歷史極值)
        // 情況 A：如果影子堆疊是空的，代表這是整個系統的第一個數字。
        // 它沒有對手，所以它自己就是「目前的最小值」。
        if (min_stk.empty()) {
            min_stk.push(value);
        } 
        // 情況 B：系統裡已經有舊數字了。
        else {
            // 拿「新進來的值 (value)」去跟「過去的最小值 (min_stk.top())」進行 PK。
            // 誰比較小，誰就是「此時此刻的最新最小值」，並將其推入影子堆疊。
            // 這樣可以保證 main_stk 和 min_stk 的高度永遠 100% 一致。
            int current_min = min(value, min_stk.top());
            min_stk.push(current_min);
        }
    }
    
    void pop() {
        // 🛡️ 防禦性設計：確保堆疊內有東西才能進行移除，避免存取越界 (Segfault)。
        if (!main_stk.empty() && !min_stk.empty()) {
            // 【關鍵物理連動】：
            // 主堆疊丟掉最上面的數字，代表我們「回到上一個狀態」。
            // 影子堆疊也必須同步丟掉最上面的極值，這樣才能完美倒轉回上一個歷史時刻的最小值。
            main_stk.pop();
            min_stk.pop();
        }
    }
    
    int top() {
        // 直接查看主堆疊的頂端，獲取最後放進去的真實數據。
        if (!main_stk.empty()) {
            return main_stk.top();
        }
        return 0; // 防呆機制：若堆疊為空時的預設回傳值
    }
    
    int getMin() {
        // 🎯 魔法發生點：
        // 我們完全不需要去主堆疊裡面掃描找最小值。
        // 因為影子堆疊的最上面，永遠存放著「對應目前主堆疊狀態」的最小值。
        // 時間複雜度完美壓在 O(1)。
        if (!min_stk.empty()) {
            return min_stk.top();
        }
        return 0; // 防呆機制
    }
};