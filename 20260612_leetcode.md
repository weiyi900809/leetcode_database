39. Combination Sum

class Solution {
public:
    // 核心遞迴引擎 (Backtracking)
    void backtrack(vector<int>& candidates, int target, int current_sum, int start_index, vector<int>& path, vector<vector<int>>& result) {
        
        // 🛑 終止條件 1：爆表 (Base Case - Pruning)
        // 如果當前總和已經超過目標，這條路走死了，直接退回上一層
        if (current_sum > target) {
            return; 
        }
        
        // 🛑 終止條件 2：達標 (Base Case - Success)
        // 找到一組完美的組合，把當前的籃子 (path) 複製並存入結果清單，然後退回
        if (current_sum == target) {
            result.push_back(path); 
            return;
        }

        // 🔄 選擇與回溯 (狀態轉移的單向門)
        // 使用 start_index 當作起點，確保迴圈只會「原地拿」或「往右拿」，絕對不走回頭路
        for (int i = start_index; i < candidates.size(); i++) {
            
            // 1. 選：把目前的數字放進籃子裡
            path.push_back(candidates[i]); 
            
            // 2. 遞迴：帶著最新的狀態進入下一層探索
            // 【關鍵點】傳入 current_sum + candidates[i] 讓下一層知道最新總和。
            // 【關鍵點】傳入 i 作為下一層的起點，代表可以「無限次重複拿同一個數字」。
            backtrack(candidates, target, current_sum + candidates[i], i, path, result);
            
            // 3. 復原 (Backtrack)：從下一層退回來時，把剛剛放進去的數字拿出來，換下一個數字試試看
            path.pop_back();
        }
    }

    vector<vector<int>> combinationSum(vector<int>& candidates, int target) {
        // 1. 準備系統變數：空的籃子 (path) 與結果清單 (result)
        vector<int> path;
        vector<vector<int>> result;
        
        // 2. 按下啟動開關：從總和 0、陣列第 0 個位置開始探索
        backtrack(candidates, target, 0, 0, path, result);
        
        // 3. 回傳最終蒐集到的所有組合
        return result;
    }
};