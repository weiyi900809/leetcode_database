12. Integer to Roman

class Solution {
public:
    string intToRoman(int num) {
        // 1. 【字典區 / 資料定義】
        // 將羅馬數字的「面額」與「符號」嚴格配對，並強制「由大到小」排序。
        // 核心技巧：將 6 個特例 (如 4, 9, 40...) 直接視為「合法的自創面額」，
        // 這樣後續的邏輯區就不需要再寫任何 if-else 來特判。
        int values[] = {1000, 900, 500, 400, 100, 90, 50, 40, 10, 9, 5, 4, 1};
        string symbols[] = {"M", "CM", "D", "CD", "C", "XC", "L", "XL", "X", "IX", "V", "IV", "I"};
        
        string result = "";
        
        // 2. 【邏輯區 / 貪婪換鈔機制】
        // 外層迴圈 (for)：代表店員的視線，從抽屜裡「最大面額的鈔票」開始往右掃描
        for (int i = 0; i < 13; i++) {
            
            // 內層迴圈 (while)：代表店員的手，只要客人的餘額還大於等於這張鈔票的面額
            // 就「重複不斷」地抽出同一種鈔票給客人，直到餘額小於該面額為止
            while (num >= values[i]) {
                num -= values[i];       // 物理動作：從餘額中扣除該面額
                result += symbols[i];   // 物理動作：將對應的羅馬符號加進答案裡
            }
            
            // 3. 【邊界防衛與效能優化】
            // 如果錢已經找平了 (餘額歸零)，就提早結束迴圈。
            // 避免浪費時間去檢查後面那些更小的硬幣 (如 1 塊錢)。
            if (num == 0) break;
        }
        
        return result;
    }
};