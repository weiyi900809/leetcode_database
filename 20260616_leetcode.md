55. Jump Game

class Solution {
public:
    bool canJump(vector<int>& nums) {
        // max_reach 記錄「歷史以來手電筒能照到的最遠 index」
        // 初始狀態站在起點，最遠可達邊界就是 0
        int max_reach = 0;
        
        // 探路者迴圈：依序走訪陣列中的每一個格子
        for (int i = 0; i < nums.size(); i++) {
            
            // 🛡️ 生存檢查 (踩坑防禦)：
            // 如果目前走到的位置 i，已經超過了光束的最遠極限 (max_reach)
            // 代表我們踩進了黑暗(物理上不可能走到這裡)，直接宣告挑戰失敗
            if (i > max_reach) {
                return false;
            }
            else {
                // 🎯 貪婪轉移方程式：
                // 既然現在這格 i 是亮的 (走得到)，就撿起地上的手電筒 (nums[i])
                // 看看「當前位置 + 能跳距離」能不能把最遠邊界推得更遠
                max_reach = max(max_reach, nums[i] + i);
            }
        }
        
        // 如果能平安無事地把整個陣列掃描完，代表從頭到尾都沒有斷層
        // 順利通關！
        return true;
    }
};