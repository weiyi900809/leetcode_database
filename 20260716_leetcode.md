173. Binary Search Tree Iterator

/**
 * Definition for a binary tree node.
 * struct TreeNode {
 * int val;
 * TreeNode *left;
 * TreeNode *right;
 * TreeNode() : val(0), left(nullptr), right(nullptr) {}
 * TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 * TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class BSTIterator {
public:
    //  【資料結構與狀態變數】
    // values: 用來儲存「壓扁」後的整棵樹 (保證是由小到大排序)
    vector<int> values;
    
    // curr_index: 讀取游標，負責記錄現在人家呼叫 next() 時，該給出哪一個數字
    int curr_index = 0;

    //  【建構子 (Constructor)】
    // 當物件被實體化時，這段程式碼會立刻執行
    BSTIterator(TreeNode* root) {
        // 防禦性設計：確保每次新建構時，狀態都是乾淨的
        values.clear();
        curr_index = 0;
        
        // 啟動 DFS (深度優先搜尋)，把整棵樹化作一維陣列
        inorder_traversal(root, values);
    }
    
    //  【核心遞迴：中序遍歷 (Inorder Traversal)】
    // 物理意義：左 -> 中 -> 右
    void inorder_traversal(TreeNode* node, vector<int>& values){
        // 遞迴終止條件：走到空節點就回頭
        if(node == nullptr){
            return;
        }
        
        // 1. 先鑽到最左邊 (找出最小的)
        inorder_traversal(node->left, values);
        
        // 2. 處理中間節點 (把數字存進陣列排隊)
        values.push_back(node->val);
        
        // 3. 鑽到右邊 (處理比較大的)
        inorder_traversal(node->right, values);
    }

    //  【取得下一個最小元素】
    int next() {
        // 安全檢查：確保游標還在陣列範圍內 (不大於最後一個 index)
        if(curr_index <= values.size() - 1){
            // return values[curr_index]; 的同時，把游標往後推一格 (++)
            // 這是 C++ 中非常簡潔優雅的後置遞增寫法
            return values[curr_index++];
        }
        // 防呆回傳
        return -1;
    }
    
    // 【檢查是否還有下一個元素】
    bool hasNext() {
        // 只要游標還沒越過陣列的最後一個元素，就代表還有貨可以給
        if(curr_index <= values.size() - 1){
            return true;
        }
        else{
            return false;
        }
    }
};