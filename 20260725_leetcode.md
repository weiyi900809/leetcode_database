98. Validate Binary Search Tree

/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
public:
    // 🔄 【階段一：中序遍歷 (Inorder Traversal)】
    // 物理意義：按照「左 -> 中 -> 右」的順序，將樹狀結構攤平，依序存入陣列中。
    void helper(TreeNode* node, vector<int>& arr){
        
        // 🛡️ 遞迴終止條件：踩到空節點，直接回頭
        if(node == nullptr) return;
        
        // 1. 鑽入左子樹 (收集比較小的數字)
        helper(node->left, arr);
        
        // 2. 處理當前節點 (將自己存入陣列排隊)
        arr.push_back(node->val);
        
        // 3. 鑽入右子樹 (收集比較大的數字)
        helper(node->right, arr);
    }
    
    bool isValidBST(TreeNode* root) {
        
        // 📦 宣告一個倉庫，用來存放攤平後的整棵樹
        vector<int> inorder_arr;
        
        // 啟動 DFS，把樹轉化為一維陣列
        helper(root, inorder_arr);
        
        // ==========================================
        // 🔍 【階段二：線性驗證 (Linear Verification)】
        // ==========================================
        // 根據 BST 的定義，攤平後的陣列必須是「嚴格由小到大遞增」。
        for(int i = 1; i < inorder_arr.size(); i++){
            
            // 🎯 【極度關鍵：嚴格遞增的判斷】
            // 如果後面的數字「小於或等於」前面的數字，代表這棵樹違反了 BST 規則。
            // ⚠️ 注意：必須包含等於 (<=)！因為標準的 BST 不允許有重複的數值。
            if(inorder_arr[i] <= inorder_arr[i - 1]){
                return false; // 發現違規，瞬間宣判死刑
            }
        }
        
        // 撐過所有檢查，這是一棵完美的 BST
        return true;
    }
};