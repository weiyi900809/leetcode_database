45. Jump Game II
/*
1. 終點起跳陷阱 (The Finish Line Trap)
直覺寫法： for (int i = 0; i < nums.size(); i++)

慘痛代價： 如果你在前面的某一步，跳躍極限 current_end 剛好精準落在陣列的最後一格。
當迴圈走到最後一格時，會觸發 i == current_end，導致你的程式在已經抵達終點的情況下，又莫名其妙地做了一次 jumps++，最後算出的答案會比正確答案多 1。

嚴謹解法： 迴圈提早一步結束 (nums.size() - 1)。只要踏進終點範圍就不再做事。
*/
class Solution {
public:
    int jump(vector<int>& nums) {
        // 邊界防禦：如果陣列長度小於等於 1 (起點即終點)，不需要任何跳躍
        if (nums.size() <= 1) return 0;
        
        int jumps = 0;       // 記錄總跳躍次數 (水波擴散的圈數 / 買車票的次數)
        int max_reach = 0;   // 探勘變數：記錄在當前跳躍範圍內，未來「最遠」能觸及的 index
        int current_end = 0; // 邊界變數：記錄「目前這一次跳躍」所能到達的極限 index

        // 核心陷阱防禦：迴圈條件必須是 i < nums.size() - 1
        // 因為一旦抵達最後一格 (終點)，遊戲就結束了，絕對不能在終點觸發「買下一張票」的機制
        for (int i = 0; i < nums.size() - 1; i++) {

            // 1. 沿路探勘：站在當前位置 i，評估潛力，更新全局最遠可達邊界
            max_reach = max(max_reach, nums[i] + i);

            // 2. 邊界結算：當迴圈走到「目前這一次跳躍」的極限盡頭時
            if (i == current_end) {
                current_end = max_reach; // 把下一跳的極限，更新為剛剛探勘到的最遠站牌
                jumps++;                 // 確定必須進行「下一跳」，跳躍次數 +1
            }
        }
        
        return jumps;
    }
};