217. Contains Duplicate


class Solution {
public:
    bool containsDuplicate(vector<int>& nums) {
        // 使用 unordered_set 作為歷史紀錄簿，只存 Key，極致節省空間
        unordered_set<int> myunordered_set;
        
        for(int i = 0; i < nums.size(); i++){
            // 條件判斷：去集合裡面查這個數字出現過幾次
            // 因為是 Set，count 的結果只會是 0 (沒看過) 或 1 (看過)
            if(myunordered_set.count(nums[i]) == 0 ){
                // 安全！把這個數字加入歷史紀錄中
                myunordered_set.insert(nums[i]);
            }
            else{
                // 抓到了！歷史紀錄裡已經有這個數字，觸發提早煞車，直接回傳 true
                return true;
            }
        }
        
        // 整個陣列都掃描完畢，沒有任何人重複，回傳 false
        return false;
    }
};