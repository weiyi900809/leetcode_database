110. Balanced Binary Tree



/**
 * Definition for a binary tree node.
 * struct TreeNode {
 * int val;
 * TreeNode *left;
 * TreeNode *right;
 * TreeNode() : val(0), left(nullptr), right(nullptr) {}
 * TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 * TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
public:
    // 【輔助函數】負責計算節點高度，並利用回傳值傳遞系統狀態
    // 正常情況回傳高度 (>= 0)；若發現子樹不平衡，則發送損壞訊號 (-1)
    int checkHeight(TreeNode* root) {
        // 1. 遞迴終止條件 (Base Case)
        // 空節點的樹高為 0，這也是最底層葉節點往下探索時的煞車點
        if(root == nullptr) {
            return 0;
        }
        
        // 2. 處理左子樹 (Post-order Traversal 的第一步)
        int left_height = checkHeight(root->left);
        // 🛑 【效能極限優化：提早煞車】
        // 如果左子樹已經回報 -1 (不平衡)，立刻中斷目前流程並向上回報 -1
        // 絕對不浪費任何 CPU 資源去走訪右子樹
        if (left_height == -1) return -1;
        
        // 3. 處理右子樹 (Post-order Traversal 的第二步)
        int right_height = checkHeight(root->right);
        // 🛑 【效能極限優化：提早煞車】
        // 同理，如果右子樹壞了，立刻向上回報
        if(right_height == -1) return -1;
        
        // 4. 處理當前節點 (Post-order Traversal 的最後一步)
        // 走到這裡，代表左右兩邊的子部門各自都是健康的
        // 現在由主管 (當前節點) 親自校驗：左右兩邊的高度差是否大於 1？
        if(abs(left_height - right_height) > 1) {
            // 高度差超過 1，系統在此節點失去平衡，發射損壞訊號
            return -1;
        }
        else {
            // 一切完美平衡！計算並回傳包含自己在內的總高度給上一層老闆
            // 總高度 = 左右兩邊較深的那一邊 + 1 (自己這一層)
            return max(left_height, right_height) + 1;
        }
    }
    
    // 【主函數】對外開放的 API 接口
    bool isBalanced(TreeNode* root) {
        // 啟動檢測引擎，並接收最終的回報結果
        int result = checkHeight(root);
        
        // 將系統狀態代碼轉譯為布林值 (Boolean)
        // 如果結果不是 -1，代表整棵樹健康平衡，回傳 true；反之則為 false
        return !(result == -1);
    }
};