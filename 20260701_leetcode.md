
452. Minimum Number of Arrows to Burst Balloons

class Solution {
public:
    int findMinArrowShots(vector<vector<int>>& points) {
        
        // 防呆機制
        if (points.empty()) return 0;
        
        // 依照開始時間排序
        // 💡 【大局觀】：將氣球由左到右排好，讓視線單向掃描，確保每次遇到新氣球時，它的左邊界都不會後退。
        sort(points.begin(), points.end(), [](const vector<int>& a, const vector<int>& b){
            return a[0]<b[0];
        });
        
        // 📦 【狀態追蹤器：有效狙擊區間】
        // curr_intersection 記錄著「如果要一箭射穿目前收集到的所有氣球，箭必須落在哪個範圍內」
        // 初始狀態：第一支箭的有效範圍，就是第一顆氣球的完整區間
        vector<int> curr_intersection=points[0];
        
        // 先把第一支箭扣在弦上
        int result=1;
        
        // 🔄 【貪婪掃描：盡可能用一支箭串起最多氣球】
        for(int i = 1; i < points.size(); i++){
            vector<int> curr_point=points[i];
            
            // 🎯 【判定是否能一箭雙鵰 (或多鵰)】
            if(curr_intersection[1] >= curr_point[0]){ 
                /*
                因為是用左端排序 所以可以保證說新來的curr_point[0]一定大於等於目前交集curr_intersection[0]
                所以就只要確保curr_intersection[1] >= curr_point[0] 的話 就是重疊了
                */
                
                // ✂️ 【交集縮圈機制：極限逼近】
                // 為了讓同一支箭也能射破這顆新氣球，我們必須把「有效狙擊區間」縮小到所有氣球的重疊處
                curr_intersection[0]=max(curr_intersection[0],curr_point[0]);//左界要向右收
                curr_intersection[1]=min(curr_intersection[1],curr_point[1]);//右界要向左收
            }
            else{//不重疊 curr_intersection 要更新  
                
                // 🏹 【極限到了，射箭並轉移目標】
                // 新氣球的起點已經超過了我們的有效狙擊右界。
                // 沒辦法，前面那群氣球只能先射爆了 (消耗 1 支箭)。
                // 接著把狙擊目標切換到這顆新氣球，並準備下一支箭。
                curr_intersection=points[i];
                result++;
            }
        }
        return result;

        
    }
};