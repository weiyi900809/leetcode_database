2. Add Two Numbers


/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {
public:
    ListNode* addTwoNumbers(ListNode* l1, ListNode* l2) {
        // 📦 【虛擬頭節點 (Dummy Node)】
        // 為了避免處理第一個節點的邊界條件（例如 result list 一開始是空的），
        // 我們在 Stack 上宣告一個實體物件 dummy。
        // 最後回傳答案時，只要回傳 dummy.next 即可。
        ListNode dummy; 
        
        // 📍 【走訪指標】
        ListNode* p1 = l1;      // 用來走訪 l1 的指標
        ListNode* p2 = l2;      // 用來走訪 l2 的指標
        ListNode* cur = &dummy; // 用來串接新節點的指標，一開始指向 dummy
        
        int carry = 0; // 進位值，初始為 0
        
        // 🔄 【主要迴圈】
        // 只要 l1 或 l2 還有節點，或者還留有進位 (carry > 0)，就必須繼續產生新節點
        while(carry > 0 || p1 != nullptr || p2 != nullptr){
            
            // 1. 安全地取得當前節點的值，若指標為空則視為 0
            int p1_value = (p1 == nullptr) ? 0 : p1->val;
            int p2_value = (p2 == nullptr) ? 0 : p2->val;
            
            // 2. 計算總和與新的進位
            int sum = p1_value + p2_value + carry;
            carry = sum / 10;
            
            // 3. 🎯 【核心觀念：在 Heap 上動態配置新節點】
            // [錯誤寫法]：ListNode node1(sum % 10); 
            // 這是將物件配置在 Stack 上。當這一輪 while 迴圈結束時，node1 就會被銷毀。
            // 若寫 cur->next = &node1; 會導致 cur->next 指向一塊無效的記憶體（懸掛指標）。
            //
            // [正確寫法]：使用 new 配置在 Heap 上
            // `new ListNode(sum % 10)` 會在 Heap 上建立一個新物件，並回傳其「記憶體位址」。
            // 我們用一個指標 `node1` 來接住這個位址。因為是配置在 Heap 上，所以即使迴圈結束，該物件依然存在。
            ListNode* node1 = new ListNode(sum % 10);
            
            // 4. 串接與移動指標
            cur->next = node1;   // 將目前的尾巴指向剛建立的新節點
            cur = cur->next;     // 將 cur 往前移動一步，準備迎接下一個節點
            
            // 5. 推進 p1 與 p2
            if(p1 != nullptr){
                p1 = p1->next;
            }
            if(p2 != nullptr){
                p2 = p2->next;
            }
        }
        
        // 返回真正的起點（跳過 dummy 節點）
        return dummy.next;
    }
};