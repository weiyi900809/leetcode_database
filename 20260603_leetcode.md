class Solution {
public:
    vector<int> intersect(vector<int>& nums1, vector<int>& nums2) {
        // 【倉庫實體庫存】用 Hash Map 記錄每個數字出現的次數
        unordered_map<int, int> map;
        vector<int> result;

        // 1. 盤點 nums1，建立庫存清單 (使用範圍迴圈，語義更清晰)
        for(int num : nums1){
            map[num]++; // 如果 Key 不存在，C++ 會自動幫你建立並初始化為 0，然後 +1
        }
        
        // 2. 拿著 nums2 的訂單去查庫存
        for(int num : nums2){
            // 如果這個商品在倉庫裡還有庫存 (> 0)
            if(map[num] > 0){
                // 成功出貨：庫存減 1，並加入出貨清單 (result)
                map[num]--;
                result.push_back(num);
            }
        }
        
        return result;
    }
};



----
----


### 🚨 系統設計連環追問 (The Follow-ups)

這才是這題真正的精華。在 Google 或 Meta 的面試中，面試官看到你寫出上面這段程式碼後，會立刻拋出以下三個極端情境來考驗你的系統架構底蘊。

請把這三個情境的對應策略刻進腦海裡：

#### 👑 追問一：空間極限優化

> **面試官：「如果這兩個陣列傳進來的時候，已經是『排好序』的狀態了，你要怎麼優化你的演算法？」**

* **你的回答：** 「既然已經排好序，我會**徹底放棄 Hash Map** 來節省 $O(M)$ 的記憶體空間。我會改用『雙指標 (Two Pointers)』。準備兩根指標 `p1` 和 `p2` 分別指著兩個陣列的開頭：
1. 若 `nums1[p1] == nums2[p2]`，代表找到交集，收進答案，雙雙前進。
2. 若 `nums1[p1] < nums2[p2]`，代表 `p1` 太小了，`p1` 往前走一步。
3. 反之 `p2` 往前走。
這樣時間複雜度一樣是 $O(M+N)$，但空間複雜度完美降到了 $O(1)$。」



#### 👑 追問二：記憶體資源分配

> **面試官：「如果 `nums1` 的長度是 100，而 `nums2` 的長度是 10,000,000，你的 Hash Map 策略要怎麼調整？」**

* **你的回答：** 「我會寫一個 `if` 判斷，確保**永遠拿『長度比較小』的那個陣列去建立 Hash Map**。以這個例子來說，就是把 `nums1` 做成 Map。這樣我的記憶體 (RAM) 只要開銷 $O(100)$ 的空間。這不僅能避免 Out of Memory，還能讓整個 Hash Map 塞進 CPU 的 L1/L2 快取 (Cache) 裡，極大地提升比對速度。」

#### 👑 追問三：巨量資料 (Big Data) 的極限環境

> **面試官：「承上題，如果 `nums2` 的資料量高達數百 GB，連記憶體 (RAM) 都塞不下，只能放在硬碟裡，你該怎麼辦？」**

* **你的回答：** 「我會採用『串流讀取 (Streaming)』或分塊讀取的策略。既然 `nums1` 比較小，我依然把它留在記憶體裡建立 Hash Map。接著，我開啟硬碟中 `nums2` 的檔案，**每次只讀取一行 (或一個 Chunk) 進記憶體**，去 Hash Map 裡面查表、扣庫存，然後把這一行丟掉。這樣就算 `nums2` 有 1TB 大，我的記憶體依然只承受 `nums1` 的大小，完美解決硬碟與記憶體的 I/O 瓶頸。」

