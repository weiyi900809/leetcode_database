118. Pascal's Triangle


class Solution {
public:
    vector<vector<int>> generate(int numRows) {
        vector<vector<int>> result;
        
        // 1. 處理 Base case：帕斯卡三角形的塔頂永遠是 [1]
        result.push_back({1});
        
        // 2. 從第二層開始往下蓋 (curr_level 代表目前正在蓋第幾層，由 2 走到 numRows)
        for(int curr_level = 2; curr_level <= numRows; curr_level++) {
            
            // 【核心技巧：預先分配記憶體與預設值】
            // 直接宣告一個長度為 curr_level 的陣列，並將所有元素預設為 1。
            // 這樣頭尾自然就是 1，迴圈內就不必再寫 if (i==0) 等繁瑣的邊界判斷。
            vector<int> curr(curr_level, 1); 
            
            // 【效能極致優化：使用 const Reference (&)】
            // result.back() 取得上一層的陣列。
            // 加上 & 代表「唯讀參考」，這能避免在記憶體中將上一層的資料重新深拷貝 (Deep Copy) 一份，
            // 對於大型二維陣列來說，這行能省下極大的時間與空間開銷。
            const vector<int>& prev = result.back();
            
            // 3. 狀態轉移：只計算「中間」的數字 (從 Index 1 走到 倒數第二格)
            // 帕斯卡三角形公式：目前格子的值 = 上一層正上方的值 + 上一層左上方的值
            for(int i = 1; i < curr_level - 1; i++) {
                curr[i] = prev[i] + prev[i-1];
            }
            
            // 4. 把填寫完畢的這一層推進最終的二維陣列中
            result.push_back(curr);
        }
        
        return result;
    }
};



---

### 🚨 你的專屬踩坑備忘錄 (動態陣列必考雷區)

這題你親自踩過了 C++ 新手最容易犯的幾個記憶體操作地雷，請把這三個坑深深印在腦海裡：

#### 💣 坑點一：對未分配空間的 Vector 直接使用 `[i]` (Segmentation Fault)

* **案發現場：** 宣告 `vector<int> curr;` 後，直接在迴圈寫 `curr[i] = 1;`。
* **災難後果：** C++ 的 `vector` 不會自動擴容來迎合你的 `[i]` 操作。因為初始長度為 0，對第 `i` 格賦值就是「非法存取未分配的記憶體」，系統會直接把程式強制關閉（Null Pointer / Out of Bounds）。
* **肌肉記憶：** 想要用 `[i]` 快速寫入資料，**宣告時一定要指定大小**（例如 `vector<int> curr(size)`）；如果不指定大小，就只能乖乖用 `push_back()` 一個一個推入。

#### 💣 坑點二：忘記處理「初始狀態」導致存取空指標

* **案發現場：** 宣告了空的 `vector<int> prev;`，但在第一圈迴圈時，`prev` 還沒有被賦予任何實體資料，就直接執行 `prev[i] + prev[i-1]`。
* **災難後果：** 一樣是記憶體越界崩潰。任何需要「回頭看歷史資料」的演算法（如動態規劃），都必須非常嚴謹地給予第一筆合法資料。
* **肌肉記憶：** 在處理具有歷史依賴的陣列操作時，先手動把 `Base Case`（如本題的 `result.push_back({1});`）塞好，再開始跑迴圈。

#### 💣 坑點三：無意識的深拷貝 (Deep Copy) 拖垮效能

* **案發現場：** 迴圈最後寫 `prev = curr;`，或是直接寫 `vector<int> prev = result.back();`。
* **災難後果：** 在 Java 或 Python 中這只是指標的轉移，但在 C++ 中，沒有加 `&` 的等號賦值，就是**把整個陣列在記憶體中重新影印一份**。如果帕斯卡三角形有一萬層，你每一層都在瘋狂影印，系統效能會瞬間崩盤。
* **肌肉記憶：** 只要你需要讀取一個現有的 `vector` 且「不打算修改它」，永遠記得加上 `const` 與 `&`（例如 `const vector<int>& prev`）。這是面試官判斷你是不是 C++ 內行的關鍵指標。605. Can Place Flowers


class Solution {
public:
    bool canPlaceFlowers(vector<int>& flowerbed, int n) {
        int counter = n;

        // 【極端測資防禦】如果目標是種 0 朵花，代表不需要種，直接回傳 true (成功)
        if(n == 0) return true;
        
        for(int i = 0; i < flowerbed.size(); i++){
            
            // 條件 1：首先確保自己腳下這個位置是空的，才有資格繼續檢查左右
            if(flowerbed[i] == 0 ){
                
                // 條件 2：【左側防護盾】
                // 如果自己在最左邊 (i==0)，左邊視為安全；否則檢查左邊那格是不是空的。
                // 這裡利用了 || 的短路特性，只要 i==0 成立，就不會執行 flowerbed[i-1]，完美避開越界！
                if(i == 0 || flowerbed[i-1] == 0 ){
                    
                    // 條件 3：【右側防護盾】
                    // 如果自己在最右邊 (i==size-1)，右邊視為安全；否則檢查右邊那格是不是空的。
                    if(i == flowerbed.size()-1 || flowerbed[i+1] == 0 ){
                        
                        // 三關全過，成功種下一朵花！
                        // 【關鍵狀態更新】必須把這裡標記為 1，否則下一圈檢查左邊時會誤判
                        flowerbed[i] = 1; 
                        counter--;
                        
                        // 【效能優化：提前煞車】
                        // 只要達到目標數量，就不需要再浪費時間掃描後面的陣列了，直接收工
                        if(counter <= 0){
                            return true;
                        }
                    }
                }
            }
        }
        
        // 整個陣列都掃完了，還是沒達到目標數量，宣告失敗
        return false;
    }
};


119. Pascal's Triangle II



class Solution {
public:
    vector<int> getRow(int rowIndex) {
        // 【極限壓縮】只宣告一條一維陣列，它既是上一層的歷史紀錄，也是這一層的新答案
        vector<int> result;
        
        // 外層迴圈：一層一層往下蓋，總共會蓋 rowIndex + 1 層
        for(int i = 0; i <= rowIndex; i++) {
            
            // 每往下蓋一層，陣列長度就會多 1。我們直接在尾巴預先塞入一個 1 (右邊界)
            result.push_back(1);

            // 【核心魔法：由右至左原地覆寫 (In-place Update)】
            // i-1 是倒數第二個數字的 Index。
            // 為什麼要從右邊算回來？因為右邊的舊資料用完就可以直接蓋掉，不會影響左邊還沒算的數字！
            // 為什麼條件是 j >= 1？因為 Index 0 (最左邊界) 永遠是 1，絕對不能去動它。
            for(int j = i - 1; j >= 1; j--) {
                // 狀態轉移：自己 = 舊的自己 (正上方) + 左邊的鄰居 (左上方)
                result[j] = result[j] + result[j - 1];
            }
        }
        
        return result;
    }
};