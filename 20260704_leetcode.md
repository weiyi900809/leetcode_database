150. Evaluate Reverse Polish Notation

class Solution {
public:
    int evalRPN(vector<string>& tokens) {
        /*
         * 🛡️ 【系統前提保證 (Constraints) 說明】
         * 本程式省略常規的防呆機制，完全基於題目敘述明確給出的嚴格數學保證：
         * 1. 保證是合法的 RPN 運算式：這意味著，每當你遇到一個運算符號 (+, -, *, /) 時，
         *    Stack 裡面絕對、必然已經存有至少兩個以上的數字。所以不可能發生「Stack 為空卻被 pop」的狀況。
         * 2. 保證不會發生除以零：測試測資中，所有的 / 運算，其分母 (被 pop 出來的第一個數字) 絕對不會是 0。
         * 3. 保證數值範圍：所有的運算過程與最終結果，都能完整放進 32-bit 帶符號整數 (int) 中，不會發生溢位報錯。
         */
        
        // 📦 【狀態容器】
        // 使用 stack 來暫存數字。為了保險起見使用 long long，避免中間運算過程潛在的極端溢位。
        stack<long long> stk;
        
        // 🔄 【線性掃描】遍歷每一個字串 (可能是數字，也可能是符號)
        for(const string& s : tokens){
            
            // ⚔️ 狀況 A：遇到加法
            if(s == "+"){
                int second_num = stk.top(); stk.pop(); // 先出來的是「右運算元」
                int first_num = stk.top(); stk.pop();  // 後出來的是「左運算元」
                int result = first_num + second_num;
                stk.push(result); // 算完的結果要丟回堆疊，等待後續運算
            }
            // ⚔️ 狀況 B：遇到減法 (⚠️ 注意順序)
            else if(s == "-"){
                // 減法沒有交換律，順序極度重要！
                // 由於堆疊是「後進先出 (LIFO)」，最上面的一定是「減數 (右邊的數字)」
                int second_num = stk.top(); stk.pop(); 
                int first_num = stk.top(); stk.pop();  // 這個才是「被減數 (左邊的數字)」
                int result = first_num - second_num;
                stk.push(result);
            }
            // ⚔️ 狀況 C：遇到乘法
            else if(s == "*"){
                int second_num = stk.top(); stk.pop();
                int first_num = stk.top(); stk.pop();
                // 乘法最容易發生溢位，這裡強制轉型成 long long 接住結果
                long long result = (long long)first_num * second_num; 
                stk.push(result);
            }
            // ⚔️ 狀況 D：遇到除法 (⚠️ 注意順序)
            else if(s == "/"){
                // 除法一樣沒有交換律，先彈出來的是「除數 (分母)」，後彈出來的是「被除數 (分子)」
                int second_num = stk.top(); stk.pop();
                int first_num = stk.top(); stk.pop();
                int result = first_num / second_num;
                stk.push(result);
            }
            // 📥 狀況 E：遇到數字
            else {
                // 將字串型態的數字轉換成整數，並推進堆疊中「等候處理」
                int number = stoi(s);
                stk.push(number);
            }
        }
        
        // 🏁 【輸出結果】
        // 當整個陣列掃描完畢，按照 RPN 的數學定義，堆疊中一定會「剛好剩下一個數字」，那就是最終答案
        return stk.top();
    }
};