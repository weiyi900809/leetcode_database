933. Number of Recent Calls

class RecentCounter {
public:
    // 宣告一個 Queue 作為滑動時間視窗的載體
    queue<int> q; 

    RecentCounter() {
        // C++ 會自動呼叫 queue 的預設建構子，無需額外處理
    }
    
    int ping(int t) {
        // 1. 收件：無論如何，先將新的請求時間加入佇列尾端
        q.push(t);

        // 2. 歷史審查 (防禦性編寫)：
        // 當佇列不為空，且排頭的最舊資料超出了 3000 毫秒的及格線時
        while(!q.empty() && q.front() < (t - 3000)) {
            // 將過期的歷史資料踢除
            q.pop();
        }
        
        // 3. 結算：迴圈結束後，留在佇列內的皆為合法區間內的請求，直接回傳數量
        return q.size();
    }
};