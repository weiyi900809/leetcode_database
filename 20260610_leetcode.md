209. Minimum Size Subarray Sum

class Solution {
public:
    int minSubArrayLen(int target, vector<int>& nums) {
        int left = 0;
        int current_sum = 0;
        int min_len = INT_MAX; // 初始化為極大值，方便後面用 min() 更新最小值

        // 【外層迴圈】右指標右移：毛毛蟲的頭往前伸，不斷吸收新資源
        for (int right = 0; right < nums.size(); right++) {
            current_sum += nums[right]; // 納入右指標指向的數值

            // 【內層迴圈】當資源溢出 (滿足 >= target) 時，左指標右移
            // 毛毛蟲的尾巴往前縮，試著精簡資源以找出「最短長度」
            while (current_sum >= target) {
                
                // 1. 🎯 只要達標，立刻計算當前視窗長度並更新歷史最小值
                min_len = min(min_len, right - left + 1);
                
                // 2. 🐛 吐出最左邊的舊資源：先從總和扣除數值，再將指標右移
                current_sum -= nums[left];
                left++;
            }
        }
        
        // 🛑 【安全防禦機制】
        // 如果跑完迴圈發現 min_len 依然是 INT_MAX，代表整個陣列加起來都小於 target
        // 依據題目要求，此時必須回傳 0；否則回傳我們找到的最短長度。
        if (min_len == INT_MAX) {
            return 0;
        } else {
            return min_len;
        }
    }
};