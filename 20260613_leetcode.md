80. Remove Duplicates from Sorted Array II

class Solution {
public:
    int removeDuplicates(vector<int>& nums) {
        // 邊界防禦：如果陣列長度小於等於 2，絕對不可能有數字出現超過 2 次，直接回傳原長度即可。
        if (nums.size() <= 2) return nums.size();
        
        // 寫入指標 (slow)：負責記錄下一個「合法數字」要填寫的空格位置。
        // 因為前兩個數字 (index 0 和 1) 無論如何都合法，所以從 index 2 開始準備寫入。
        int slow = 2;
        
        // 探路指標 (fast)：負責掃描陣列中的每一個數字，從 index 2 開始一路往右檢查。
        for (int fast = 2; fast < nums.size(); fast++) {
            
            // 核心防護網：檢查 fast 手上的數字，是否與「即將寫入位置的前兩格 (slow - 2)」不同。
            // 如果不同，代表把 fast 寫進去後，絕對不會產生三個連續相同的數字，因此合法。
            if (nums[slow - 2] != nums[fast]) {
                nums[slow] = nums[fast]; // 將合法數字覆寫到 slow 的位置
                slow++;                  // 寫入完成，slow 往前推進一格，準備接下一個數字
            }
        }
        
        // 迴圈結束後，slow 的值剛好等於新陣列的長度 (即合法數字的總個數)
        return slow;
    }
};