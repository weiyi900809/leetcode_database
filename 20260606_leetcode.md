1700. Number of Students Unable to Eat Lunch


class Solution {
public:
    int countStudents(vector<int>& students, vector<int>& sandwiches) {
        // 【空間降維打擊】放棄模擬實體 Queue/Stack，改用「需求計數法」(O(1) 空間)
        // student_prefer[0] 紀錄喜歡圓形(0)的人數，student_prefer[1] 紀錄喜歡方形(1)的人數
        vector<int> student_prefer(2,0);
        
        // 指向目前三明治堆疊 (Stack) 最頂端的指標
        int now_no_sandwich = 0; 
        
        // 1. 系統盤點：統計所有學生的需求總量
        // 因為不喜歡的學生會無限次重新排隊，代表學生的「排隊順序」毫無意義，只需掌握「總數」
        for(int i = 0; i < students.size(); i++){
            student_prefer[students[i]]++;
        }
        
        // 2. 依序出貨：從最頂端的三明治開始往下發放
        while(now_no_sandwich < sandwiches.size()){
            
            // 檢查：如果隊伍中還有「任何一個」學生喜歡目前這個頂端的三明治
            if(student_prefer[sandwiches[now_no_sandwich]] > 0 ){
                // 成功出貨：消耗掉一個對應的學生需求
                student_prefer[sandwiches[now_no_sandwich]]--;
                // 推進指標：準備檢查下一顆三明治
                now_no_sandwich++;
            }
            // 【系統死結 (Deadlock) 偵測】
            // 如果所有排隊的學生，都沒有人喜歡目前頂端這顆三明治了
            else{
                // 這顆三明治會永遠卡在最上面，系統陷入死結，強制中斷出貨作業
                break;
            }
            
            /*
            【架構師防錯筆記】
            在同一個迴圈中，如果你修改了陣列的 Index (例如 now_no_sandwich++)，
            請絕對不要在同一個迴圈的後續程式碼中，再次無條件使用這個 Index 去存取陣列，
            除非你重新做了一次邊界檢查。使用 else 是最優雅且安全的做法。
            */
        }
        
        // 3. 系統結算：回傳吃不到飯的人數
        // 將剩下的需求數量相加，即為無法被滿足的學生總數
        return student_prefer[0] + student_prefer[1];
    }
};