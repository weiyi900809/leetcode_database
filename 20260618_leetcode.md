274. H-Index



class Solution {
public:
    int hIndex(vector<int>& citations) {
        // 1. 降序排序 (Descending Order)
        // 創造「只要這篇達標，前面所有篇數絕對也達標」的絕對數學優勢
        sort(citations.begin(), citations.end(), greater<int>());

        // 2. 將 i 宣告在外層作用域 (Scope)
        // 確保迴圈結束後，我們還能讀取到 i 的最終數值
        int i = 0; 
        
        // 3. 逐一比對：i 代表「目前陣列的 index」，而 i + 1 代表「這篇論文的排行 (Rank)」
        for(i = 0; i < citations.size(); i++) {
            
            // 尋找斷層：如果「這篇論文的引用次數」已經撐不起「它目前的排行」
            // 代表黃金交叉已經發生，後面的論文引用數只會更低，不可能再達標了
            if(citations[i] < i + 1) {
                break; // 發現極限，立刻終止掃描
            }
        }
        
        // 4. 回傳極限值：
        // 無論是遇到斷層 break，還是陣列全部掃描完畢自然結束
        // 殘留下來的 i，在數學上精準等於「成功達標的論文總篇數」，也就是 H-Index
        return i;
    }
};