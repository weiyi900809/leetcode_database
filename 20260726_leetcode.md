130. Surrounded Regions


class Solution {
public:
    // 🔄 【核心遞迴：DFS 感染傳播】
    // 物理意義：我們是一支從邊界出發的搜救隊。
    // 只要看到 'O'，就把它標記為 'S' (Safe/Saved)，並繼續向四周擴散尋找相連的 'O'。
    void dfs_helper(vector<vector<char>>& board, int row, int colu){
        int m = board.size();
        int n = board[0].size();
        
        // 🛡️ 【防護網：邊界檢查】
        // 如果搜救隊走出了地圖邊界，直接回頭
        if(row < 0 || row >= m || colu < 0 || colu >= n ) return;
        
        // 🛑 【終止條件：遇到牆壁或已救援目標】
        // 如果這格不是 'O' (也就是遇到了 'X' 牆壁，或是已經被標記過的安全區 'S')，停止探索
        if(board[row][colu] != 'O'){
            return;
        } 
        else {
            // 🎯 【狀況 A：發現未救援的 'O'】
            // 1. 標記為安全狀態 'S' (Safe)
            board[row][colu] = 'S';
            
            // 2. 繁衍探索：向上下左右四個方向繼續擴散感染
            dfs_helper(board, row - 1, colu); // 往上
            dfs_helper(board, row + 1, colu); // 往下
            dfs_helper(board, row, colu - 1); // 往左
            dfs_helper(board, row, colu + 1); // 往右
        }
    }
    
    void solve(vector<vector<char>>& board) {
        // 防呆機制：如果地圖是空的，直接結束
        if(board.empty() || board[0].empty()) return;
        
        int m = board.size();
        int n = board[0].size();
        
        // ==========================================
        // 🚨 【階段一：邊界掃描 (Boundary Scanning)】
        // 物理意義：被 'X' 包圍的 'O' 絕對不可能與邊界相連。
        // 所以我們只需要巡視地圖的「四個邊緣」，把與邊緣相連的 'O' 全部保護起來。
        // ==========================================
        int i, j;
        
        // 掃描左邊界 (colu = 0)
        j = 0;
        for(i = 0; i < m; i++){
            dfs_helper(board, i, j);
        }
        
        // 掃描右邊界 (colu = n - 1)
        j = n - 1;
        for(i = 0; i < m; i++){
            dfs_helper(board, i, j);
        }
        
        // 掃描上邊界 (row = 0)
        i = 0;
        for(j = 0; j < n; j++){
            dfs_helper(board, i, j);
        }
        
        // 掃描下邊界 (row = m - 1)
        i = m - 1;
        for(j = 0; j < n; j++){
            dfs_helper(board, i, j);
        }

        // ==========================================
        // ⚖️ 【階段二：全盤結算 (Final Resolution)】
        // 物理意義：經過邊界搜救後，地圖上的狀態已經非常明確：
        // 'S' -> 被救下的 'O' (連通到邊界)
        // 'O' -> 沒被救到的 'O' (絕對是被死死包圍的，必須死)
        // 'X' -> 原本的牆壁
        // ==========================================
        for(i = 0; i < m; i++){  
            for(j = 0; j < n; j++){
                
                // 將受保護的 'S' 還原為題目要求的 'O'
                if(board[i][j] == 'S'){
                    board[i][j] = 'O';
                }
                // 剩下的情況 ('O' 或是原本的 'X')，一律變成 'X'
                // 這個 else 寫得非常乾淨，直接把未連通的 'O' 蓋掉，也省去了額外的判斷！
                else {
                    board[i][j] = 'X';
                } 
            }
        }
        
        return;
    }
};