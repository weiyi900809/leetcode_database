134. Gas Station

class Solution {
public:
    int canCompleteCircuit(vector<int>& gas, vector<int>& cost) {
        int total_tank = 0;   // 第一宇宙法則：記錄全宇宙的總盈虧
        int current_tank = 0; // 第二宇宙法則：記錄當前起點的局部油箱
        int start_station = 0; // 預計的起點，一開始設為 0
        
        for (int i = 0; i < gas.size(); i++) {
            int net_energy = gas[i] - cost[i];
            
            total_tank += net_energy;   // 全局累加
            current_tank += net_energy; // 局部累加
            
            // 🛡️ 破產檢查：嚴格小於 0 才算失敗
            if (current_tank < 0) {
                // 帶著前面的存款都死在這一站了，代表前面所有站點全部報廢！
                // 把希望寄託在下一站
                start_station = i + 1; 
                current_tank = 0; // 白手起家，新起點油箱歸零
            }
        }
        
        // 🏁 最終大局結算與「免回頭定理」的極端值防禦
        // 💡 靈魂拷問：如果後半段好不容易走完，但賺的油根本不夠賠前半段的大坑怎麼辦？
        // 💡 嚴謹解答：如果「後半段賺的資產」不夠賠「前半段欠的債務」，
        //            這兩者加起來的 total_tank 必定會是負數 ( < 0 )！
        //            所以那種極端情況，會直接在下面這個 if 被精準攔截。
        if (total_tank < 0) {
            return -1; // 總資源不夠（包含後半段填補不了前半段的情況），絕對無解
        }
        else {
            // 只要能走到這一步 (total_tank >= 0)，數學上絕對保證：
            // 「後半段賺的油」一定 大於或等於 「前半段欠的油」！
            // 因此，帶著後半段累積的存款繞回 index 0，去走前半段的坑，絕對不會熄火破產。
            return start_station;
        }
    }
};