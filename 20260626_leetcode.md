54. Spiral Matrix

class Solution {
public:
    vector<int> spiralOrder(vector<vector<int>>& matrix) {
        // 🛡️ 【極端邊界防衛】
        // 如果傳入的是空矩陣，或者矩陣裡面沒有任何元素，直接回傳空陣列
        if(matrix.empty() || matrix[0].empty()) return {};
        
        // 取得矩陣的總列數 (n) 與總行數 (m)
        int n = matrix.size();
        int m = matrix[0].size();
        
        // 📦 【定義四大物理邊界】
        // 想像這四個變數是四道牆壁，把矩陣包圍起來
        int top = 0;          // 上邊界 (天花板)
        int bottom = n - 1;   // 下邊界 (地板)
        int left = 0;         // 左邊界 (左牆)
        int right = m - 1;    // 右邊界 (右牆)
        
        vector<int> result;
        
        // 🔄 【核心邏輯：四壁不斷向內收縮】
        // 只要天花板還沒碰到地板，且左牆還沒碰到右牆，遊戲就繼續
        while (top <= bottom && left <= right) {
            
            // ➡️ 步驟 1：【向右走】貼著天花板 (top)，從左牆走到右牆
            for (int i = left; i <= right; i++) {
                result.push_back(matrix[top][i]); 
            }
            // 走完一整列後，最上面的那一列已經被刮乾淨了，所以天花板要「往下降一格」
            top++;
            
            // 🛡️ 防衛檢查：如果天花板降得太低，超過了地板，代表所有數字都掃完了，立刻終止！
            if(top > bottom || left > right) break;
            
            // ⬇️ 步驟 2：【向下走】貼著右牆 (right)，從天花板走到地板
            for (int i = top; i <= bottom; i++) {
                result.push_back(matrix[i][right]); 
            }
            // 走完一整行後，最右邊的那一行已經被刮乾淨了，所以右牆要「往左縮一格」
            right--;
            
            // 🛡️ 防衛檢查：確保右牆往內縮之後，沒有和左牆交叉
            if(top > bottom || left > right) break;
            
            // ⬅️ 步驟 3：【向左走】貼著地板 (bottom)，從右牆走到左牆
            for (int i = right; i >= left; i--) {
                result.push_back(matrix[bottom][i]); 
            }
            // 走完一整列後，最下面的那一列已經被刮乾淨了，所以地板要「往上升一格」
            bottom--;
            
            // 🛡️ 防衛檢查：確保地板往上升之後，沒有頂穿天花板
            if(top > bottom || left > right) break;
            
            // ⬆️ 步驟 4：【向上走】貼著左牆 (left)，從地板走到天花板
            for (int i = bottom; i >= top; i--) {
                result.push_back(matrix[i][left]); 
            }
            // 走完一整行後，最左邊的那一行已經被刮乾淨了，所以左牆要「往右縮一格」
            left++;
            
            // 🛡️ 防衛檢查：確保左牆往內縮之後，洋蔥還沒剝完 (這裡的 break 可留可不留，但留著格式最統一安全)
            if(top > bottom || left > right) break;
        }
        
        return result;
    }
};