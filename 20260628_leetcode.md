49. Group Anagrams

class Solution {
public:
    vector<vector<string>> groupAnagrams(vector<string>& strs) {
        vector<vector<string>> result;
        
        // 📦 【字典區 / 分類箱】
        // Key (string): 排序後的字串，也就是這個群組的「標準化 DNA」
        // Value (vector<string>): 擁有相同 DNA 的所有原始字串集合
        unordered_map<string, vector<string>> charMap;

        // 🔄 【第一階段：尋找特徵並分門別類】
        for (int i = 0; i < strs.size(); i++) {
            
            // 1. 複製目前的原始字串，準備進行解剖
            string curr_s = strs[i];
            
            // 2. 將字串按照字母順序重新排列，萃取出「標準化 DNA」
            // 例如："eat", "tea", "ate" 經過這行後，都會變成 "aet"
            sort(curr_s.begin(), curr_s.end());
            
            // 3. 把「原始字串」丟進對應 DNA 的分類箱裡面
            // 如果 charMap 裡面還沒有 curr_s 這個 Key，C++ 會自動幫你建立一個空的 vector 再 push_back
            charMap[curr_s].push_back(strs[i]);
        }
        
        // 📦 【第二階段：打包出貨】
        // 遍歷整個 Hash Map，把每一個分類箱裡面的 vector，全部塞進最終的 result 裡面
        // 使用 const auto& 可以避免不必要的字串拷貝，提升效能
        for (const auto& i : charMap) {
            // i.first 是排序後的字串 (DNA key)
            // i.second 是裝滿原始字串的 vector (我們只要這個)
            result.push_back(i.second);
        }
        
        return result;
    }
};