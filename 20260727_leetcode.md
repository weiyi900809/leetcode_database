133. Clone Graph
/*
// Definition for a Node.
class Node {
public:
    int val;
    vector<Node*> neighbors;
    Node() {
        val = 0;
        neighbors = vector<Node*>();
    }
    Node(int _val) {
        val = _val;
        neighbors = vector<Node*>();
    }
    Node(int _val, vector<Node*> _neighbors) {
        val = _val;
        neighbors = _neighbors;
    }
};
*/

class Solution {
public:
    // 🔄 【核心遞迴：DFS 深度優先搜尋與分身複製】
    // m_p 必須使用傳參照 (unordered_map<Node*, Node*>&)，讓所有遞迴層級共用同一本「對照字典」
    Node* helper(Node* c_node, unordered_map<Node*, Node*>& m_p){
        
        // 🛡️ 【防護網：空圖處理】
        if(c_node == nullptr) return nullptr;
        
        // 🛑 【記憶體查表：防止無窮迴圈】
        auto it = m_p.find(c_node);
        if(it != m_p.end()){
            // 如果字典裡已經存過這個舊節點 (c_node) 的位址，
            // 代表它已經被複製過了，直接回傳它對應的「新節點 (Clone) 位址」即可，立刻中斷遞迴。
            return it->second;
        }
        
        // ==========================================
        // 🏗️ 【階段一：建立新實體 (Clone)】
        // ==========================================
        // 如果找不到，代表這是一個從未見過的全新節點。
        // 我們根據舊節點的值，在 Heap 上 new 出一個獨立的新節點。
        Node* new_node = new Node(c_node->val);
        
        // 🎯 【極度關鍵的順序：先寫入字典，再處理鄰居】
        // 必須在進入鄰居的 for 迴圈"之前"，就先把 (舊位址 -> 新位址) 的對應關係存入 Map。
        // 如果你把它寫在 for 迴圈後面，當遇到 A <-> B 互相連接的狀況時，
        // A 去找 B，B 又回來找 A 時 Map 裡還是空的，就會引發無窮迴圈爆炸。
        m_p[c_node] = new_node;
        
        // ==========================================
        // 🤝 【階段二：繁衍與串接 (處理 Neighbors)】
        // ==========================================
        for(int i = 0; i < c_node->neighbors.size(); i++){
            // 派 helper 去處理舊節點的每一個鄰居。
            // helper 會負責生出那個鄰居的分身 (或從 Map 中調出已存在的分身)。
            Node* new_neighbor = helper(c_node->neighbors[i], m_p);
            
            // 把拿到的「新鄰居分身」，加入到「當前新節點」的鄰居陣列中
            new_node->neighbors.push_back(new_neighbor);
        }
        
        // 交付完整複製好 (且鄰居也綁定完畢) 的新節點
        return new_node;
    }
    
    Node* cloneGraph(Node* node) {
        
        // 📦 【基礎設施：全域對照字典 (Hash Table)】
        // Key: 原始圖的 Node 記憶體位址 (Old Address)
        // Value: 複製出來的 Node 記憶體位址 (New Address)
        // 這個 Map 同時扮演了 DFS 中 visited 陣列的角色。
        unordered_map<Node*, Node*> mp;
        
        // 啟動 clone 函數 進行全圖複製
        Node* clone_node = helper(node, mp);
        
        return clone_node;
    }
};