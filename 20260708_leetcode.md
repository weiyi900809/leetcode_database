82. Remove Duplicates from Sorted List II

/**
 * Definition for singly-linked list.
 * struct ListNode {
 * int val;
 * ListNode *next;
 * ListNode() : val(0), next(nullptr) {}
 * ListNode(int x) : val(x), next(nullptr) {}
 * ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {
public:
    ListNode* deleteDuplicates(ListNode* head) {
        // 📦 【防護網：虛擬頭節點】
        // 為了防止連原來的 head 都是重複的（例如 1->1->2），
        // 建立 dummy 節點來當作一個絕對安全的起點。
        ListNode dummy(0, head);
        
        // 📍 【雙指標分工】
        // node1 (守門員)：永遠指向「目前為止，絕對沒有重複的最後一個安全節點」
        ListNode* node1 = &dummy; 
        
        // node2 (探路者)：負責在前面開路，尋找有沒有重複的數字
        ListNode* node2 = head; 
        
        // 🔄 【主要走訪迴圈】
        while (node2 != nullptr) {
            
            // 1. 探路者開始掃描重複地雷
            // 如果下一個節點的值跟現在一樣，node2 就繼續往下走，
            // 直到 node2 停在這個「連續重複數字的最後一個節點」上。
            while(node2->next != nullptr && node2->next->val == node2->val){
                node2 = node2->next;
            }

            // 2. 結算探路者的結果
            if(node1->next == node2){
                // 🛡️ 狀況 A：安全過關
                // 守門員的下一個節點，剛好就是探路者現在的位置。
                // 代表剛剛的 while 迴圈連一次都沒進去，這個數字是獨一無二的！
                // 守門員可以安心地往前踏上這個節點。
                node1 = node2;
            }
            else{
                // 💣 狀況 B：發現連續重複的地雷區
                // node2 已經跑到前面去了，代表 node1->next 到 node2 之間全是重複的垃圾。
                // 守門員立刻放開原本牽的手，跨過這片地雷區，直接牽起 node2 後面的「未知新節點」。
                node1->next = node2->next;
                
                // ⚠️ 注意：這裡 node1 絕對不能往前走！必須留在原地等下一輪檢查。
            }
            
            // 3. 探路者繼續往下一個未知的節點邁進
            node2 = node2->next;
        }
        
        // 回傳真正的 List 起點 (跳過 dummy)
        return dummy.next;
    }
};