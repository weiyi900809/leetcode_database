128. Longest Consecutive Sequence

class Solution {
public:
    int longestConsecutive(vector<int>& nums) {
        // 📦 【資料準備區】
        // 使用 Hash Set 來儲存所有數字。
        // 目的有兩個：1. 自動剃除重複的數字；2. 讓後續尋找數字的時間複雜度降為 O(1)。
        unordered_set<int> s;
        int max_length = 0;
        
        // 📥 第一次掃描：把所有數字通通丟進 Set 裡面建立索引
        for (int i = 0; i < nums.size(); i++) {
            s.insert(nums[i]);
        }
        
        // 🔄 第二次掃描：遍歷 Set 裡面的每一個不重複元素
        for (const auto& element : s) {
            
            // 🛡️ 【核心過濾機制：尋找火車頭】
            // 檢查「比目前數字小 1 的數字 (pre)」存不存在？
            int pre = element - 1;
            
            // 只有在「找不到 pre」的情況下，才代表當前的 element 是這段連續序列的「最起點」！
            // 如果 pre 存在，代表 element 只是某段序列的中間份子，我們直接跳過，交給它的老大去算就好。
            if (s.find(pre) == s.end()) {
                
                int length = 1;         // 記錄目前連續序列的長度 (包含自己)
                int now = element + 1;  // 準備往上尋找下一個連續數字
                
                // 🚂 【順藤摸瓜：計算車廂數量】
                // 既然確定自己是起點，就開始瘋狂往上找，看這串數字能連到多長
                while (s.find(now) != s.end()) {
                    now++;      // 數字 +1，繼續找下一個
                    length++;   // 長度 +1
                }
                
                // 🏆 挑戰並更新歷史最長紀錄
                max_length = max(max_length, length);
            }
        }
        
        return max_length;
    }
};