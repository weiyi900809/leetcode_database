/**
 * Definition for a binary tree node.
 * struct TreeNode {
 * int val;
 * TreeNode *left;
 * TreeNode *right;
 * TreeNode() : val(0), left(nullptr), right(nullptr) {}
 * TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 * TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
public:
    // helper 函式負責在 inorder 陣列的 [inLeft, inRight] 視窗內，建構出對應的子樹
    // preIndex 使用 pass by reference (&)，讓它在所有遞迴層級中共享同一個進度游標
    TreeNode* helper(vector<int>& preorder, vector<int>& inorder, int inLeft, int inRight, int& preIndex) {
        
        // 🛡️ 【遞迴終止條件 (Base Case)】
        // 如果左界超越了右界，代表這個視窗內已經沒有節點了，回傳空指標 (nullptr)
        if(inLeft > inRight) return nullptr;
        
        // 1. 抓出老大 (Root)：Preorder 陣列目前游標指著的數字，絕對是當前子樹的 Root
        int rootValue = preorder[preIndex];
        preIndex++; // 游標往前推進，準備交給下一個子樹取用
        
        // ==========================================
        // 🔍 【劃分楚河漢界：在 Inorder 中尋找 Root 的位置】
        // ==========================================
        // 確立搜尋視窗的終點 (searchEnd)。因為 find 的區間是 [first, last)，不包含 last，所以要 + 1
        auto searchEnd = inorder.begin() + inRight + 1;
        
        // 在 [inLeft, inRight] 的視窗內，尋找 rootValue 的迭代器 (Iterator)
        auto it = find(inorder.begin() + inLeft, searchEnd, rootValue);
        
        /*
         * 💡 【指標算術 (Pointer/Iterator Arithmetic) 的物理意義】
         * find(...) 回傳的不是整數 Index，而是 Iterator (記憶體位置的游標)。
         * inorder.begin() 是這整塊連續記憶體的起點游標。
         * 將兩個 Iterator 相減 (`it - inorder.begin()`)，
         * 在物理上就是在計算「從起點走到該元素，總共跨越了多少個記憶體區塊」，
         * 藉此精準算出該元素在 vector 中的絕對 Index。
         */
        int rootIndex = it - inorder.begin();
        
        if (it == searchEnd) {
            // 由於題目測資保證 preorder 和 inorder 必定能構成一棵合法的樹，
            // 所以永遠不可能找不到 rootValue。此處防呆機制可省略。
        }
        
        // ==========================================
        // 🏗️ 【實體化與領土分配 (Recursive Construction)】
        // ==========================================
        // 在 Heap 上建立當前這棵樹（或子樹）的實體節點
        TreeNode* curr_root = new TreeNode(rootValue);
        
        /*
         * 🎯 【極度關鍵的執行順序：必須先左後右】
         * Preorder 的物理排列是：[Root] -> [左子樹領土] -> [右子樹領土]。
         * 因為 preIndex 是全域共享且不斷遞增的，當我們拿走 Root 後，
         * 緊接在後面的數字，"絕對"是屬於左子樹的節點。
         * 如果先呼叫 right = helper(...)，preIndex 就會把原本屬於左子樹的節點，
         * 錯誤地分配給右子樹，導致整棵樹建構崩潰。
         */
        
        // 遞迴建構左子樹：Inorder 視窗縮小為 [inLeft, rootIndex - 1]
        curr_root->left = helper(preorder, inorder, inLeft, rootIndex - 1, preIndex);
        
        // 遞迴建構右子樹：Inorder 視窗縮小為 [rootIndex + 1, inRight]
        curr_root->right = helper(preorder, inorder, rootIndex + 1, inRight, preIndex);

        // 回傳建構完成的子樹根節點
        return curr_root;
    }
    
    TreeNode* buildTree(vector<int>& preorder, vector<int>& inorder) {
        // preIndex 是全域進度條，從 0 開始
        int preIndex = 0;
        /*
        int& preIndex 的意思是：helper 裡的 preIndex 不是一份副本，而是呼叫端那個整數變數本身的別名。
        所以 helper 裡做 preIndex++ 時，外面的那個 index 也真的會加 1。
        C++ 的 reference 必須綁定到既有變數；一般的非 const reference 不能綁到像 0 這種常值或暫時結果。
        */
        
        // 啟動遞迴，初始 Inorder 視窗為一整條陣列 [0, size - 1]
        return helper(preorder, inorder, 0, inorder.size() - 1, preIndex);
    }
};