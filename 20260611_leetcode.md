560. Subarray Sum Equals K

class Solution {
public:
    int subarraySum(vector<int>& nums, int k) {
        // 紀錄簿：用來儲存歷史上出現過的「前綴和 (存摺餘額)」以及「出現次數」
        // Key: 前綴和的值, Value: 該數值出現的次數
        unordered_map<int, int> mp;
        
        // 🌟 完美的起手式 (系統初始狀態)
        // 代表在還沒讀取任何數字前，「總和為 0」這個狀態已經存在 1 次。
        // 物理意義：確保如果「從第 0 格加到現在剛好等於 k」，我們能成功翻到紀錄。
        mp[0] = 1; 
        
        int current_sum = 0; // 當前的存摺總餘額
        int count = 0;       // 符合條件的子陣列總數
        
        for (int i = 0; i < nums.size(); i++) {
            // 第一步：結算當前的總和
            current_sum += nums[i]; 
            
            // 第二步：🎯 【翻歷史紀錄簿】(順序絕對不能錯！)
            // 核心公式：當前餘額 - 目標淨利 = 過去某天的餘額 (current_sum - k)
            // 如果歷史紀錄中有這個數字，代表「從那天之後到現在」的總和剛好就是 k
            if (mp.count(current_sum - k) >= 1) {
                // 🛑 為什麼是 += 次數，而不是單純 +1？
                // 因為陣列中有負數或 0，過去可能有多個不同的起點都剛好走到相同的「餘額」。
                // 每一個起點都能跟現在的位置配對出一個合法的子陣列，所以必須「全數收割」。
                count += mp[current_sum - k];
            }
            
            // 第三步：💾 【更新紀錄簿】
            // 將自己當前的總和記錄下來，出現次數 +1，留給未來的數字去查。
            // 🛑 為什麼一定要放在第三步 (最後做)？
            // 如果 k = 0，且先更新紀錄再查表，系統算出來的 target (current_sum - 0) 就會是自己。
            // 這會導致「自己跟剛存進去的自己配對」，憑空捏造出一個不存在的子陣列！
            mp[current_sum]++;
        }
        
        return count;
    }
};