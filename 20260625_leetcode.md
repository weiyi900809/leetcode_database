11. Container With Most Water


class Solution {
public:
    int maxArea(vector<int>& height) {
        // 1. 【雙指標初始化】
        // left 站在最左邊的木板，right 站在最右邊的木板。
        // 這樣可以保證一開始的「底部寬度 (right - left)」是最大的。
        int left = 0;
        int right = height.size() - 1;
        int max_area = 0; // 記錄歷史最大儲水量
        
        // 只要左右指標還沒相遇，就繼續尋找
        while(left < right){
            
            // 2. 【計算當前容量：木桶效應】
            // 水位的高度永遠受限於「比較短」的那塊木板。
            // 面積 = (左右距離的寬) * (左右木板較短的高)
            int curr_area = (right - left) * (min(height[right], height[left]));
            
            // 挑戰並更新歷史最大紀錄
            max_area = max(max_area, curr_area);
            
            // 3. 【貪婪抉擇：嚴謹的排除法】
            // 當我們準備把指標往內縮時，寬度 (right - left) 必定會減少 1。
            // 既然「寬度」注定縮水，如果要讓「面積」變大，唯一的希望就是找到「更高」的木板。
            if(height[right] > height[left]){
                // 左邊是短板。如果我們保留短板，去移動右邊的高板，
                // 未來的水位「絕對不可能」超過目前的 height[left]，且寬度又變窄，面積必定下降。
                // 因此，唯一的活路就是捨棄目前的左邊短板，向內尋找更高的可能。
                left++;
            }
            else if(height[right] < height[left]){
                // 右邊是短板。同理，捨棄右邊的短板。
                right--;
            }
            else{
                // 兩邊一樣高。此時高度被這兩塊板子鎖死，只要寬度一縮，面積必定變小。
                // 除非未來能在內部找到「兩塊都比現在更高」的板子，否則無法打破當前紀錄。
                // 這裡無論移動 left 還是 right 都可以，因為當前的組合已經計算過了。
                left++;
            }
        }
        
        return max_area;
    }
};