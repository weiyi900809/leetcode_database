509. Fibonacci Number

class Solution {
public:
    int fib(int n) {
        // 空間極致壓縮：只宣告三個常數變數，空間複雜度 O(1)
        int before_prev = 0; // 扮演 F(n-2) 的角色
        int prev = 1;        // 扮演 F(n-1) 的角色
        int now = 0;         // 扮演 F(n) 的角色
        
        // 邊界防護機制：處理極端測資
        if(n == 0) return before_prev;
        if(n == 1) return prev;
        
        // 狀態轉移引擎啟動
        for(int i = 2; i <= n; i++){
            // 核心推導：當前狀態 = 歷史狀態的總和
            now = prev + before_prev;
            
            // 嚴密的狀態輪轉機制 (注意順序不可互換)
            before_prev = prev; // 舊的 F(n-1) 退役，變成下一回合的 F(n-2)
            prev = now;         // 新算出的 F(n) 成為下一回合的 F(n-1)
        }
        
        return now;
    }
};