236. Lowest Common Ancestor of a Binary Tree

/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode(int x) : val(x), left(NULL), right(NULL) {}
 * };
 */
class Solution {
public:
    TreeNode* lowestCommonAncestor(TreeNode* root, TreeNode* p, TreeNode* q) {
        
        // ==========================================
        // 🛡️ 【階段一：遞迴終止條件 (Base Case)】
        // 物理意義：我們是搜救隊，走到盡頭或找到目標就要回報。
        // ==========================================
        
        // 狀況 1：走到空節點（死路），回報「這裡什麼都沒有 (nullptr)」
        if(root == nullptr) return nullptr;
        
        // 狀況 2 & 3：幸運地直接踩到目標 p 或 q！
        // 直接把「我自己 (root)」回報給上層，代表「我找到其中一個了！」
        // 💡 思考：如果另一個目標剛好在我的子樹裡怎麼辦？
        // 沒關係，因為我已經是祖先了，直接回傳我，答案依然正確。
        if(root == p) return root;
        if(root == q) return root;
        
        // ==========================================
        // 🔄 【階段二：向下探索 (Divide and Conquer)】
        // 物理意義：既然我自己不是目標，那就派左右護法去子樹裡找。
        // ==========================================
        
        // 派出左小隊，去左子樹尋找 p 和 q，並把結果記錄在 left_side
        TreeNode* left_side = lowestCommonAncestor(root->left, p, q);
        
        // 派出右小隊，去右子樹尋找 p 和 q，並把結果記錄在 right_side
        TreeNode* right_side = lowestCommonAncestor(root->right, p, q);

        // ==========================================
        // ⚖️ 【階段三：向上回報 (合併左右小隊的結果)】
        // ==========================================
        
        // 狀況 A：左右小隊都兩手空空，代表這棵子樹裡根本沒有 p 也不存在 q。
        // 誠實向上層回報：「我們這裡都沒有」。
        if(left_side == nullptr && right_side == nullptr){
            return nullptr;
        }
        
        // 狀況 B：左小隊有找到人，但右小隊沒找到。
        // 代表目標（或共同祖先）全都在左邊，我們就把左小隊的成果直接往上呈報。
        if(left_side != nullptr && right_side == nullptr){
            return left_side;
        }
        
        // 狀況 C：右小隊有找到人，但左小隊沒找到。
        // 代表目標（或共同祖先）全都在右邊，把右小隊的成果直接往上呈報。
        if(right_side != nullptr && left_side == nullptr){
            return right_side;
        }
        
        // 🎯 狀況 D：左右小隊「都」回報找到人了！
        // 這就是奇蹟發生的時刻：p 和 q 分別在我的左邊和右邊。
        // 根據定義，**我 (root) 本人就是最近共同祖先 (LCA)**！
        // 這裡直接 return root 即可，不需要再寫 if 判斷，也能消弭編譯器警告。
        return root; 
    }
};