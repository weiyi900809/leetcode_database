6. Zigzag Conversion



class Solution {
public:
    string convert(string s, int numRows) {
        // 🛡️ 【邊界防衛機制】
        // 1. 如果只有 1 列，字串根本無法呈現 Z 字型，直接原封不動回傳。
        // 2. 如果字串長度比列數還小 (或相等)，連一個向下的直線都填不滿，也不用折返，直接回傳。
        // 這一步非常關鍵，它能防止後續邏輯發生 dir 變數錯亂或 Out of Bounds。
        if (numRows == 1 || numRows >= s.size()) {
            return s;
        }
        
        // 📦 【資料準備區】
        // 開發 numRows 個字串空間，把每一列 (Row) 當成一個獨立的容器
        vector<string> s_list(numRows);
        
        // 🕹️ 【物理狀態追蹤器】
        int curr_row = 0; // 紀錄我們現在正站在哪一列
        int dir = 1;      // 方向向量：1 代表「往下走」(行號增加)，-1 代表「往上走」(行號減少)
        
        // 🔄 【核心邏輯：模擬字元填入過程】
        for (int i = 0; i < s.size(); i++) {
            
            // 動作 1：把當前字元，無腦塞進我們目前站著的那一列
            s_list[curr_row] += s[i];

            // 動作 2：偵測是否「撞牆」，並及時反轉方向 (觸底反彈機制)
            if (curr_row == numRows - 1) {
                // 撞到最底部的列了，下一步必須「往上走」
                dir = -1;
            }
            else if (curr_row == 0) {
                // 撞到最頂部的列了 (天花板)，下一步必須「往下走」
                dir = 1;
            }
            
            // 動作 3：根據當前的方向，移動到下一列，準備迎接下一個字元
            curr_row = curr_row + dir;
        }
        
        // 🏁 【組裝最終答案】
        // 經過上面的模擬，每一列專屬的字元都已經嚴謹歸位。
        // 現在只需要把第 0 列到最後一列的字串，由上到下全部串聯起來即可。
        string result;
        for (int i = 0; i < numRows; i++) {
            result += s_list[i];
        }
        
        return result;
    }
};