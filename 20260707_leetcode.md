92. Reverse Linked List II

class Solution {
public:
    ListNode* reverseBetween(ListNode* head, int left, int right) {
        // 📦 【防護網：虛擬頭節點 (Dummy Node)】
        // 配置在 Stack 上的虛擬節點，用來應對 left == 1 (從頭開始反轉) 的極端情況。
        // 這樣可以確保「反轉區段的前一個節點 (node1)」永遠存在。
        ListNode dummy = ListNode(0, head);
        
        // 📍 【核心指標宣告】
        ListNode* node1; // 守門員：永遠停在反轉區段的「前一個」位置，不會移動
        ListNode* node2; // 尾巴：原本反轉區段的「第一個」節點，反轉後會慢慢被推擠變成「最後一個」

        // ==========================================
        // 🔄 【Round 1：精準定位守門員 (node1) 與尾巴 (node2)】
        // ==========================================
        int counter = left - 1; // 計算需要往前走幾步才能到達 left 的前一格
        
        // 注意！！ 一定要從 dummy 開始，這樣走 left-1 步後，才會剛好停在 left 的「前一格」
        ListNode* curr = &dummy; 
        while (counter > 0) {
            curr = curr->next;
            counter--;
        }
        
        // 抵達起跑線，確立不變的錨點
        node1 = curr;        // node1 站定在 left 的前一個節點
        node2 = curr->next;  // node2 站定在第 left 個節點 (反轉初期的頭，反轉完成後的尾)
        
        // ==========================================
        // 🔄 【Round 2：頭插法 (Head Insertion) 核心反轉】
        // ==========================================
        counter = right - left; // 計算需要執行「拔起再安插」的總次數
        
        // 每次迴圈的物理動作：把 node2 後面的那個節點拔起來，安插到 node1 的正後方
        while (counter > 0) {
            
            // 1. 抓出目標 (node3)：鎖定 node2 後面的那個節點，準備將它拔起
            // (宣告在迴圈內，確保只有在真的需要反轉時才讀取，避免記憶體越界)
            ListNode* node3 = node2->next; 
            
            // 2. 斷開連結 (斷後路)：node2 放開 node3，直接牽起 node3 後面的節點
            node2->next = node3->next; 
            
            // 3. 安插到最前面 (接新人)：node3 飛到反轉區段的最前端，接上「目前反轉區段的第一名」
            // 🎯 你完美修正的地方：必須接 node1->next，絕對不能接 node2！
            node3->next = node1->next; 
            
            // 4. 確立新起點 (立榜首)：守門員 node1 牽起剛飛過來的 node3，讓它正式成為新的第一名
            node1->next = node3;
            
            counter--;
        }
        
        // 回傳真正的 List 起點 (跳過 dummy)
        return dummy.next;
    }
};