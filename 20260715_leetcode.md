129. Sum Root to Leaf Numbers



/**
 * Definition for a binary tree node.
 * struct TreeNode {
 * int val;
 * TreeNode *left;
 * TreeNode *right;
 * TreeNode() : val(0), left(nullptr), right(nullptr) {}
 * TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 * TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
public:
    // helper 函式負責帶著「上一層累積的數值 (num)」，繼續往下探索
    int helper(TreeNode* node, int num) {
        
        // 🛡️ 【防護網與遞迴終止條件】
        // 狀況 A：如果走到空節點，代表這條路徑不存在，對總和的貢獻為 0。
        // 這個防呆機制也完美處理了「節點只有單邊子樹」的情況。
        if(node == nullptr) return 0;
        
        // 🧮 【核心數學運算：進位與累加】
        // 將上一層傳下來的數字往左推一位 (乘以 10)，並加上當前節點的值。
        // 例如：上一層傳下來 12，當前節點是 3，就會變成 12 * 10 + 3 = 123。
        num = num * 10 + node->val;
        
        // 🎯 【狀況 B：抵達終點 (Leaf Node)】
        // 判斷條件：如果當前節點既沒有左小孩，也沒有右小孩，它就是一片葉子。
        if(node->left == nullptr && node->right == nullptr) {
            // 代表這條從 Root 到 Leaf 的路徑已經走到底了，
            // 這個累積出來的數字 (num) 已經是最終成品，直接回傳向上層報告。
            return num;
        }
        else {
            // 🌿 【狀況 C：還在樹枝上 (Internal Node)】
            // 這條路徑還沒走完，而且可能面臨分岔。
            // 我們把目前累積的半成品 (num) 複製並分別交給左子樹與右子樹繼續走。
            // 最後將左邊探索到的總和，加上右邊探索到的總和，一起往上回報。
            return helper(node->left, num) + helper(node->right, num);
        }
    }
    
    int sumNumbers(TreeNode* root) {
        // 從 Root 開始往下走時，手上的初始數字為 0
        int num_start = 0;
        
        // 啟動 DFS 遞迴，並接收整棵樹加總完的最終結果
        int total = helper(root, num_start);
        
        return total;
    }
};