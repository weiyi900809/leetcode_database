73. Set Matrix Zeroes

class Solution {
public:
    void setZeroes(vector<vector<int>>& matrix) {
        // 🛡️ 【極端邊界防衛】
        // 如果矩陣是空的，直接結束，避免後續存取 matrix[0] 發生 Out of Bounds 錯誤。
        if (matrix.empty() || matrix[0].empty()) return;

        int m = matrix.size();    // 取得矩陣的總列數 (Row)
        int n = matrix[0].size(); // 取得矩陣的總行數 (Column)

        // 📝 【狀態紀錄器初始化】
        // 開發兩個一維陣列，分別用來記錄「哪一列」與「哪一行」被宣判要歸零。
        // 空間複雜度：$O(M + N)$
        vector<bool> label_row(m, false);
        vector<bool> label_column(n, false);

        // 🔍 【第一次掃描：只查閱，不動手】
        // 掃描整個矩陣，尋找「原生的 0」。
        for (int i = 0; i < m; i++) {
            for (int j = 0; j < n; j++) {
                if (matrix[i][j] == 0) {
                    // 只要在 (i, j) 發現 0，就把第 i 列與第 j 行的「歸零標記」設為 true。
                    // 這裡絕對不能直接改 matrix 的值，否則會引發連鎖汙染。
                    label_row[i] = true;
                    label_column[j] = true;
                }
            }
        }

        // 💥 【第二次掃描：根據標記，執行制裁】
        // 再次掃描整個矩陣，這次我們不看原矩陣的值了，而是看我們的「死亡筆記本」(標記陣列)。
        for (int i = 0; i < m; i++) {
            for (int j = 0; j < n; j++) {
                // 只要這格所在的「列 (i)」或「行 (j)」有任何一個被標記為 true
                // 就毫不猶豫地把這格設為 0
                if (label_row[i] || label_column[j]) {
                    matrix[i][j] = 0;
                }
            }
        }
    }
};