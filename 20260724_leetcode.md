230. Kth Smallest Element in a BST


/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
public:
    // count 與 result 使用傳參照 (int&)，在整個遞迴過程中共享同一份記憶體
    void helper(TreeNode* node, int& count, int k, int& result){
        
        // 🛡️ 【防護網】走到空節點，代表此路不通，直接回頭
        if(node == nullptr) return;
        
        // 🛑 【煞車一：進入節點前的攔截】
        // 如果在進入這個節點之前，別的分支就已經達標了，直接退回，不浪費時間。
        if(count == k) return;

        // ==========================================
        // 🔄 【中序遍歷：1. 鑽入左子樹 (尋找更小的數)】
        // ==========================================
        helper(node->left, count, k, result);
        
        // 🛑 【煞車二：從左子樹回歸時的攔截 (你的神來一筆)】
        // 剛從左子樹探險回來。如果左子樹裡面已經找到了第 k 小的元素，
        // 就絕對不能再往下執行 count += 1 (否則計數會超標並覆蓋答案)，必須立刻撤退！
        if(count == k) return;
        
        // ==========================================
        // 🔄 【中序遍歷：2. 處理當前節點 (中)】
        // ==========================================
        // 左邊都找完了，現在輪到當前這個節點。將計數器 +1。
        count += 1; 
        
        // 🛑 【煞車三：精準命中目標】
        // 如果加上當前節點剛好是第 k 個，立刻把值存入 result，並啟動撤退機制。
        if(count == k) {
            result = node->val;
            return;
        }
        
        // ==========================================
        // 🔄 【中序遍歷：3. 探索右子樹 (尋找較大的數)】
        // ==========================================
        // 如果還沒找齊 k 個，才准許往右子樹繼續尋找
        helper(node->right, count, k, result);
    }
    
    int kthSmallest(TreeNode* root, int k) {
        /*
         * 🧠 【核心思維：中序遍歷的數學特性與極致剪枝】
         * BST 的 Inorder 遍歷物理上就是「由小到大的嚴格遞增數列」。
         * 我們不需要耗費 O(N) 的時間與空間去儲存完整的陣列。
         * 透過傳遞 count 參照，每拜訪一個節點就計數，配合「三重煞車機制」，
         * 只要達標就瞬間崩解 Call Stack，讓時間複雜度最佳化至 O(H + k)。
         */
        int count = 0;
        int result = 0;
        
        helper(root, count, k, result);
        
        return result;
    }
};