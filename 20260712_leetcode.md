106. Construct Binary Tree from Inorder and Postorder Traversal

/**
 * Definition for a binary tree node.
 * struct TreeNode {
 * int val;
 * TreeNode *left;
 * TreeNode *right;
 * TreeNode() : val(0), left(nullptr), right(nullptr) {}
 * TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 * TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
public:
    // helper 函式負責在 inorder 陣列的 [inLeft, inRight] 視窗內，建構出對應的子樹
    TreeNode* helper(vector<int>& postorder, vector<int>& inorder, int inLeft, int inRight, int& postIndex) {
        
        // 🛡️ 【遞迴終止條件 (Base Case)】
        // 如果左界超越了右界，代表這個視窗內已經沒有節點了，回傳空指標 (nullptr)
        if(inLeft > inRight) return nullptr;
        
        // 1. 抓出老大 (Root)：
        // 在 Postorder 中，整棵樹（或子樹）的 Root 絕對會出現在區間的最尾端。
        // 因為我們是從後往前掃描，所以目前的 postIndex 指向的就是 Root。
        int rootValue = postorder[postIndex];
        postIndex--; // 游標「往前」推進，準備交給下一個子樹取用
        
        // ==========================================
        // 🔍 【劃分楚河漢界：在 Inorder 中尋找 Root 的位置】
        // ==========================================
        // 確立搜尋視窗的終點 (searchEnd)。因為 find 的區間是 [first, last)，不包含 last，所以要 + 1
        auto searchEnd = inorder.begin() + inRight + 1;
        
        // 在 [inLeft, inRight] 的視窗內，尋找 rootValue 的迭代器 (Iterator)
        auto it = find(inorder.begin() + inLeft, searchEnd, rootValue);
        
        // 利用指標算術 (Iterator 相減)，精準算出該 Root 在 Inorder 陣列中的絕對 Index
        int rootIndex = it - inorder.begin();
        
        if (it == searchEnd) {
            // 由於題目測資保證 postorder 和 inorder 必定能構成一棵合法的樹，
            // 所以永遠不可能找不到 rootValue。此處防呆機制可省略。
        }
        
        // ==========================================
        // 🏗️ 【實體化與領土分配 (Recursive Construction)】
        // ==========================================
        // 在 Heap 上建立當前這棵樹（或子樹）的實體節點
        TreeNode* curr_root = new TreeNode(rootValue);
        
        /*
         * 🎯 【極度關鍵的執行順序：必須先右後左】
         * Postorder 的原本遍歷順序是：[左子樹] -> [右子樹] -> [Root]。
         * 我們現在是「從後往前」讀取 postorder 陣列，所以讀取的順序變成了：
         * [Root] -> [右子樹] -> [左子樹]。
         * 因為 postIndex 是全域共享且不斷「遞減」的，當我們拿走 Root 後，
         * 緊接在前面的數字，"絕對"是屬於「右子樹」的節點。
         * 所以必須先呼叫 right = helper(...)，否則游標會分配錯亂！
         */
        
        // 遞迴建構右子樹：Inorder 視窗縮小為 [rootIndex + 1, inRight]
        curr_root->right = helper(postorder, inorder, rootIndex + 1, inRight, postIndex);

        // 遞迴建構左子樹：Inorder 視窗縮小為 [inLeft, rootIndex - 1]
        curr_root->left = helper(postorder, inorder, inLeft, rootIndex - 1, postIndex);
        
        // 回傳建構完成的子樹根節點
        return curr_root;
    }
    
    TreeNode* buildTree(vector<int>& inorder, vector<int>& postorder) {
        
        // postIndex 是全域進度條，因為是後序遍歷，Root 在最後面，所以游標從陣列最尾端開始
        int postIndex = postorder.size() - 1;
        
        // 啟動遞迴，初始 Inorder 視窗為一整條陣列 [0, size - 1]
        return helper(postorder, inorder, 0, inorder.size() - 1, postIndex);
    }
};